# Используем базовый образ Node.js
FROM node:16-alpine

# Создаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем содержимое папки app из локальной файловой системы в контейнер
COPY dist/ .

# Запускаем файл index.js
CMD ["node", "index.js"]
